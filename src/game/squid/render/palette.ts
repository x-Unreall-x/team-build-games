/**
 * Squid's own player palette. The arena moved to shape-based fighter identity and
 * dropped its color system; squid keeps colors because leg ownership must be readable
 * at a glance (each player's leg is tinted their color, grey = unheld).
 */
export const SQUID_PALETTE = [
  0x38bdf8, 0xf472b6, 0xa3e635, 0xfbbf24, 0xc084fc, 0xfb7185, 0x34d399, 0xf97316,
];
